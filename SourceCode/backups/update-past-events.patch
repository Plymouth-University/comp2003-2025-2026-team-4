*** Begin Patch
*** Update File: SourceCode/Style.css
@@
-body {
-    font-family: 'Inter', Arial, sans-serif;
+body {
+    /* Use site font from the UI CheatSheet (Aptos). Fallback to Inter/Arial if not available */
+    font-family: var(--font-family);
     font-size: 16px;
     line-height: 1.5;
 }
 
 /* COLOURS */
 .bg-black { background-color: #000000; }
 .bg-yellow { background-color: #F0FF70; }
 .bg-pink { background-color: #FFF9FC; }
 .bg-white { background-color: #FFFFFF; }
+
+/* Design tokens from SaltyPadel_UI_CheatSheet */
+:root {
+  --color-yellow: #F0FF70;
+  --color-pink: #FFF9FC;
+  --color-black: #000000;
+  --font-family: 'Aptos', 'Inter', Arial, sans-serif;
+}
+
+/* Optional: Add Aptos font files to assets/fonts/ and uncomment to load locally */
+/*
+@font-face {
+  font-family: 'Aptos';
+  src: url('assets/fonts/Aptos-Regular.woff2') format('woff2');
+  font-weight: 400;
+  font-style: normal;
+  font-display: swap;
+}
+*/
@@
-/* ========== PAGES (hidden by default) ========== */
-#who_we_are,
-#what_we_do,
-#upcoming_events,
-#past_events,
-/* ============== Login page stuff ============== */
+# ========== PAGES (hidden by default) ========== */
+# Hide SPA pages by default
+# (display is toggled by JS)
+# who/what/past/upcoming pages
+# =========================
+#who_we_are,
+#what_we_do,
+#upcoming_events,
+#past_events {
+  display: none;
+  padding: 64px 24px;
+  min-height: 50vh;
+}
+
+/* ============== Login page stuff ============== */
@@
 #edit_home_page,
 #add_new_event {
     padding: 64px 24px;
     min-height: 50vh;
 }
+
+/* === Past Events (merged from past_events.css) === */
+.page-banner { background: var(--color-yellow); padding: 32px 60px; }
+.page-banner h1 { margin: 0; font-size: 40px; font-weight: 500; }
+
+.gallery {
+  padding: 30px 60px;
+  display: grid;
+  grid-template-columns: repeat(2, 1fr);
+  gap: 30px;
+}
+.gallery img {
+  width: 100%;
+  height: 320px;
+  object-fit: cover;
+  border-radius: 18px;
+  display: block;
+}
+
+.join-page { display: flex; justify-content: center; margin: 60px 0; }
+.join-page-animation {
+  background-color: var(--color-yellow);
+  border: none;
+  border-radius: 999px;
+  padding: 12px 28px;
+  font-size: 16px;
+  font-weight: 600;
+  cursor: pointer;
+  box-shadow: 0 2px 6px rgba(0,0,0,0.2);
+  transition: transform 0.2s ease, box-shadow 0.2s ease;
+}
+.join-page-animation:hover { transform: scale(0.94); }
+
+@media (max-width: 700px) {
+  .gallery { grid-template-columns: 1fr; gap: 18px; padding: 20px; }
+  .page-banner { padding: 20px; }
+  .page-banner h1 { font-size: 28px; }
+}
*** End Patch